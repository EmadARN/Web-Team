/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["backend-adlikara.ir"], // دامنه خود را اینجا اضافه کنید
  },
};

export default nextConfig;
